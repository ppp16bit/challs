Challenge description [CHALL.md](CHALL.md)

In **docker-compose.yml** you can modify the **WORKER COUNT** variable to change the number of workers the application will run

docker image: [DockerHub](https://hub.docker.com/r/pppedro/magalu-chall)

Run:
```bash
docker compose up -d
```